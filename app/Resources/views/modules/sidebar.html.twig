<h4>Les autres observations de cette espèce</h4>
{% for observation in observations %}
    {% if loop.index > 1 %}
        {% if observation.user != null %} {{ observation.user.username }} {% else %}Inconnu{% endif %} - {{ observation.createdAt|date('d-m-Y') }} - <a href="{{ path('app_observation', {'id' : observation.id}) }}">Voir</a>
        <br>
    {% endif %}
{% endfor %}